machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
  override:
    - docker info
    - if [[ -e ~/docker/image.tar ]]; then docker load --input ~/docker/image.tar; fi
    - docker build -t malice_totalhash .
    - mkdir -p ~/docker; docker save malice_totalhash > ~/docker/image.tar
test:
  override:
    - docker run malice_totalhash --user test --key test dcdca9f1ef23c06cc077bf1179e94fa999827b64

deployment:
  # master:
  #   branch: master
  #   commands:
  #     - eval $(docker run gliderlabs/pagebuilder circleci-cmd)
  release:
    branch: release
    commands:
      - make release
